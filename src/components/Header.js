import pic from '../pics/header.jpeg'

const header = () => {
    return (
        <header>
            <img src={pic}></img>
            <h1>My Website Header!</h1>
        </header>
    )
}

export default header
